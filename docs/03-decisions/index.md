# Decisions — ADR registry

The architectural invariants that protect email-2-data, one decision per ADR. This index is
the discoverability surface; read it top-to-bottom once, refer back when a change touches an
invariant. ADRs are **immutable once Accepted** — supersede with a new ADR, don't edit history
(`adr-000-template.md` is the template).

If your change touches an ADR's invariant, the ADR must hold or be explicitly superseded by a
new ADR in the same commit. A new architectural rule means a new ADR + a row here.

| ADR | Slug | One-line invariant |
| --- | --- | --- |
| [001](adr-001-compute-proportional-to-uncertainty-impact.md) | compute-proportional-to-uncertainty-impact | Spend compute (tokens, model power) in proportion to `uncertainty × business impact` — the governing principle. |
| [002](adr-002-read-only-imap-guarantee.md) | read-only-imap-guarantee | IMAP is read-only, enforced twice: `EXAMINE` + `BODY.PEEK[]`; STORE/EXPUNGE/DELETE/APPEND/COPY forbidden. Pinned by `test_fetch_safety.py`. |
| [003](adr-003-counterparty-from-body-not-domain.md) | counterparty-from-body-not-domain | The body decides counterparty (CLIENT/SUPPLIER/LEAD from Lindo's POV); domain is a prior, never a verdict. Never silently bin a client. |
| [004](adr-004-direction-by-recipient-not-sender.md) | direction-by-recipient-not-sender | Direction is its own header-derived axis; outbound mail is classified by the recipient, not the sender. |
| [005](adr-005-gazetteer-is-prior-not-verdict.md) | gazetteer-is-prior-not-verdict | The gazetteer is a prior attached to the LLM input; any gazetteer knowledge vetoes an offline IGNORE. |
| [006](adr-006-two-tier-cascade-anti-ignore-guardrail.md) | two-tier-cascade-anti-ignore-guardrail | Tier-0 header signals bin obvious bulk offline (free); only header signals may IGNORE, and only when no gazetteer hint exists; uncertain → NEEDS_REVIEW. |
| [007](adr-007-nif-iban-authoritative-rest-candidates.md) | nif-iban-authoritative-rest-candidates | Deterministic extraction: NIF/IBAN are FACT (validated); amounts/dates/docs are candidates (INFERENCE), never auto-promoted. |
| [008](adr-008-every-verdict-records-decided-by.md) | every-verdict-records-decided-by | Every `TriageResult` stamps `decided_by` (tier + engine/version) so verdicts are debuggable, auditable, replayable. |
| [009](adr-009-incremental-idempotent-by-default.md) | incremental-idempotent-by-default | fetch/triage/sync are incremental (UID watermark in `sync.db`) and idempotent; never re-spend Tier-1 tokens on processed mail. `--full` overrides. |
| [010](adr-010-workspace-db-precious-vs-regenerable.md) | workspace-db-precious-vs-regenerable | `workspace.db` is precious (human decisions) and never auto-rebuilt; `crm.db`/`sync.db` are regenerable; orphaned refs surface as dangling. |
| [011](adr-011-export-honesty-boundary.md) | export-honesty-boundary | Export sends only the job shell (brief), never costed lines; always an explicit human action; refuses non-estimable / re-export without `--force`. |
| [012](adr-012-shared-llm-provider-dispatch.md) | shared-llm-provider-dispatch | All LLM access goes through `llm.py` (provider dispatch + retry-on-empty); provider configurable (vertex_gemini default / anthropic). |
| [013](adr-013-client-email-composer-deterministic.md) | client-email-composer-deterministic | The Projetos client-email composer is deterministic (no LLM), server-assembled from `jobspec.askables` + the editable `client_email_template.md`, reviewed in an editable draft, and never auto-sent. |
| [014](adr-014-restful-deep-linkable-cockpit-urls.md) | restful-deep-linkable-cockpit-urls | Cockpit views are deep-linkable: detail resources carry their id in the path (`/projetos/<pid>`, `/contrapartes/<key>`, 404 on unknown); list filters + inline state ride in the query (`?counterparty=`, `?thread=`). |
| [015](adr-015-knowledge-capture-claim-ledger.md) | knowledge-capture-claim-ledger | Off-email knowledge (calls/meetings/decisions) is one deterministic, desktop, provenance-rich claim ledger reusing `project_field_history` (reserved `__kind__` rows); conflict = contradiction (tie at winning rank), not supersession; custom fields are tier=context and never gate estimability. |
| [016](adr-016-post-audit-resilience-hardening.md) | post-audit-resilience-hardening | Resilience/token hardening from the perf audit (strengthens, supersedes none): fresh-volume boot never crashes; a failing account never starves the others; a Tier-1 failure escalates to `NEEDS_REVIEW` (never disappears); the Gemini playbook prefix + the reply draft are cached not re-billed; dedup is folder-independent without a Message-ID; container ops hygiene (fail-loud port, `/healthz` + `HEALTHCHECK`). |
| [017](adr-017-project-close-out-lifecycle.md) | project-close-out-lifecycle | `CANCELLED` is a first-class terminal stage; cancelling/losing a project records a queryable close-out (party ∈ client/supplier/our + reason + closed_at, v4 columns), cleared on reopen. Distinct from delete (which destroys the record). |
| [018](adr-018-multi-owner-and-in-app-roster.md) | multi-owner-and-in-app-roster | Ownership is a SET on both Fila threads and projects (v4 join tables; single owner backfilled); the roster is editable in-app (`settings.team` ∪ a `roster` table, effective per request). A read-only participants rollup surfaces the ADR-015 `asserted_by` ledger. |
| [019](adr-019-conversational-intake-capture-adapter.md) | conversational-intake-capture-adapter | The Telegram intake bot is a capture adapter on the ADR-015 ledger (no new store); a pluggable cloud/local engine narrows ADR-015 §Decision-4 **for this path only** (owner-signed, ADR-020); scope = the staffer's own dictation + artifacts, not covert client-call recordings; deterministic-first + model-by-uncertainty (seeded from the email playbooks); no auto-apply (the user is the gatekeeper); a capture is never dropped. |
| [020](adr-020-capture-egress-and-data-handling.md) | capture-egress-and-data-handling | Owner-signed (2026-06-16) capture egress (Telegram transit + EU cloud inference on `materials-492723`); minimise-at-edge (persist-then-scrub from Telegram; retry-until-confirmed + reconciliation sweep deferred) and preserve-at-core (full fidelity appended to the project for audit, media attached); the scrubbed Telegram is not a fallback, so `captures/` is sole-copy precious and must be backed up; Telegram-backend erasure is a disclosed residual (~48 h / private-chat limit). |
| [021](adr-021-intake-lan-binding-minimal-auth.md) | intake-lan-binding-minimal-auth | *(**Accepted** 2026-07-25 — LAN bind + auth gate shipped in ADR-039, which replaced the single shared secret with per-person accounts and made the LAN bind opt-in)* For intake validation the webapp would relax loopback-only → LAN-only (port 8042 unchanged) behind a minimal single-user auth gate (the app's first auth; secret in `.env`); "never public" stays firm — no webhook/inbound port: the worker long-polls outbound and writes via the store seam, bypassing the gate (this part **shipped**). |
| [022](adr-022-capture-revision-chain.md) | capture-revision-chain | *(Proposed — not built; see the [execution plan](../04-implementation/capture-revision-chain-plan.md))* Re-capture and content correction are ONE model: a **successor capture row**, never an in-place edit or a reopened row. `applied`/`discarded` stay terminal **for the row they name**, so the M3 double-write + apply-after-discard guards stay byte-identical; reopen is whitelisted to `status=='applied'` alone. Correction as a revision is how ADR-020 preserve-at-core is honoured (the sole copy is never overwritten; the model's words sit beside the human's correction). Intent (`corrigir`/`acrescentar`/`refazer`) is **recorded, never inferred** — no reversal event is auto-written. Filing into a second obra is a separate additive action, not a re-capture. Corrects an earlier misreading: ADR-015 §2 does **not** forbid `source_mid="capture:<cid>"` — ADR-020 blesses it by name. |
| [023](adr-023-live-decision-queue-freshness.md) | live-decision-queue-freshness | The decision lenses stay live by **moving the data**, not by busting caches: a periodic background sync (`sync.interval_minutes`, default 15) joins startup + the button as an ingestion trigger, reusing `_run_sync` so it inherits the skip-if-busy lock and never-raises contract; affordable because the ADR-009 watermark makes an idle tick cost 0 tokens. `/para-ti` polls itself every 30 s and swaps items **in place** (never `location.reload()`), keyed by CONTENT not list index — an index re-points at a different decision once a refresh reorders the queue. Both routes send `no-store`; the page shows how old the mail actually is. |
| [024](adr-024-decision-carries-its-evidence.md) | decision-carries-its-evidence | A gate in **Para ti** expands **in place** into the evidence behind it — the full email thread (shared `msgThreadHTML` kit), its attachments, and what extraction already knows — so a decision can be judged without leaving the queue. Accordion (one open, addressable via `?item=`/`?tipo=`); **one round-trip** (`/api/thread` carries a lazy `spec` block, keyed off the thread's CRM rows, so the ADR-023 poll never pays for it). The spec folds **per field, never per line item** — per-item rendering buried the known values under ~25 identical "em falta" rows — with partial coverage stated ("150 · 1 de 5") and provenance explicit (ADR-007). Reclassify / owner / handled act on the card itself. All remembered state (open, dismissed, focus) is content-keyed so an ADR-023 refresh can reorder underneath it. |
| [025](adr-025-attachment-guards-and-scoped-re-extraction.md) | attachment-guards-and-scoped-re-extraction | A thin project was a **crash**, not a weak model: a 283 MP attachment (1 MB vector export) passed the byte-only filter, Vertex 400'd every retry, `build_entry` swallowed it, the entities fallback made it look sparse, and the incremental gate froze it forever. Images are now guarded by **pixels** via a header-only probe (unmeasurable formats pass — dropping a readable drawing on a guess is worse); **SVG routes to text** (it is geometry, and below the image byte floor); a failed pass sets `spec_error` + a `spec_draft_failed` audit event; `rebuild_jobspecs(only=…)` re-extracts one project without re-billing the corpus; `llm.with_tier` picks light/standard/heavy **per call on a copy** (mutating shared `settings` would repoint every later call), and `thinking_budget` is no longer hardcoded to 0. Verified: same `standard` model, coverage 0.22 → 0.56. |
| [026](adr-026-re-extraction-reads-the-whole-project.md) | re-extraction-reads-the-whole-project | "Reprocessar" re-reads **everything the project knows**, not just its emails: the ADR-025 email pass plus a new pass over the ADR-015 timeline events (Registar notes, decisions, calls, WhatsApp), at one chosen **cost** tier. Events run oldest→newest **after** the messages, so a written-down note beats a passively-parsed email. A field read out of a note is written as `source_mid="event:<rowid>"` with **no** channel/asserted_by — a fifth case in the ADR-022 §7 vocabulary, on the *machine* side: attributing it to the note's author would freeze a model guess inside `human_touched_fields` and render it as a FACT nobody confirmed. ADR-025's three safety properties hold unchanged (human decisions skipped, nothing deleted, identical values no-op → idempotent). A note the model could not read is reported (`events.failed`), never swallowed — hence `extract_fields_strict` beside the capture path's degrading wrapper. Cost is stated before the click and split `read` vs `applied` after; blank notes never reach the model; a project with only notes and no emails is now serviceable. Registar's placeholder narrows from "sem IA" to "a IA só lê isto se pedires Reprocessar" — the storage is still verbatim, but the blanket promise was no longer true. |
| [027](adr-027-ai-polish-sits-on-top-of-the-deterministic-draft.md) | ai-polish-sits-on-top-of-the-deterministic-draft | The "melhorar tom" layer ADR-013 reserved, built — **extending** ADR-013, not superseding it. Button-only (`#_aibtn`; `loadDraft` provably cannot reach it): the server rebuilds the deterministic draft from the ticked keys and hands **that** to the model with four fenced blocks (draft · questions · confirmed facts minus internal flags · last 6 thread messages @1200 chars), so the questions enter the prompt as a list the model did not choose. "Sits on top of" is then **checked**: `missing_questions` verifies every question survived verbatim (whitespace-normalised — a re-wrap passes, a reword fails) and names any that did not, in a warning telling the user not to send it. Both texts are returned and adopting is a second click, so the polish never silently becomes the draft. Own editable playbook (`client_email_polish_playbook.md`, fallback carries the same hard rules), per-call cost tier, and loud failure — 502/503 rather than returning the unpolished draft as a success. |
| [028](adr-028-decisions-persist-and-stay-reviewable.md) | decisions-persist-and-stay-reviewable | A disposition is a precious human decision: Para-ti "Ignorar" persists (v8 `para_ti_dismissals`, content-keyed, thread never binned), `Z` undo is universal with honest failure copy (`falhou` vs `falhou — revertido`), and *tratado* threads stay reviewable in the Fila's **Tratados ledger** (`?include=resolved`, reopen with undo, `?thread=` deep-links fall back to it). Machine keys never render as names (v8 `counterparty_names` override → contact-name fallback → key; projects renameable). Automated senders (`signals.NO_REPLY_RE`) never get a "Criar projeto" gate. Companion queue-legibility fixes: risk chip = filter, sticky order, only critical red (≥72 h) pulses, visible owner filter, reply draft from the queue. |
| [029](adr-029-fila-groups-by-obligation.md) | fila-groups-by-obligation | The Fila partitions on **obligation** before anything else: sticky sections **Precisam de resposta** (`WE_OWE`) / **À espera deles** (`AWAITING`) / **Internos**, each with a count. `_band()` encodes *urgency only* — a fresh we-owe and a fresh awaiting are both green — so with the default `Mais recentes` sort the two opposite obligations interleaved and were separable only by reading Portuguese prose; the queue read as 79 tasks when much of it was already with the client. Group is the primary key via a **stable** partition in `view()`, so the chosen sort survives *within* a section (no second sort key). Row level carries it too: **hollow dot = waiting on them**, colour still urgency, so a row read outside its section still says whose move it is. "À espera deles" is deliberately muted — a status report, not a to-do list. The Tratados ledger stays flat (all `HANDLED`). `state` was already on every row; nothing new is derived, only rendered — and the data contract is pinned separately, because losing `state` would sweep the whole queue into `Internos` and make a busy queue look calm. |
| [030](adr-030-descricao-composer-deterministic-with-house-style.md) | descricao-composer-deterministic-with-house-style | The DESCRIÇÃO composer (`descdraft.py`) drafts the proposta/fatura product descritivo from **confirmed** JobSpec fields, then an optional LLM polish sits on top and is checked — the ADR-013/-027 pattern on a new surface, wired to the Projetos page (`GET/POST …/description[/polish]`). A 59-PDF read-only sweep + analysis (`out/wording-analysis/`) found **no single house style** (three coexist, two in one author's same-day docs), so "keep the same style across all documents" is a **normalisation** requirement: the playbook renders the **corpus AVERAGE** — the modal form on each dimension (prose 39/59, header + 1 sentence, opener `Produção de`, `em {material}`, header passed through as typed not upper-cased) as `{processo} {item} em {material}, c/ {dimensoes}, {acabamento}.`. This **reversed** a first cut that fixed the *labelled* block (best-reproduced ≠ most-representative; the user asked for the average, which is prose). Every value comes from a confirmed field; an unconfirmed LLM draft is absent (`require_confirmed`); a missing fact renders an **inline visible gap** `em [[MATERIAL?]]`, never a silent omission. The internal `process` field never reaches client prose (opener default `Produção de`). `missing_facts`/`dropped_gaps` verify the polish kept every measurement verbatim and tidied no gap away. Standardises **going forward**; reproduces no individual past document (by design). |
| [031](adr-031-client-email-purpose-selector-and-verbatim-fact-guard.md) | client-email-purpose-selector-and-verbatim-fact-guard | The client-email composer gains a **purpose selector** (8 kinds — ask · reject · quote · follow-up · approval · payment · deadline · ready), `ask` unchanged as the default. Each purpose's base draft stays deterministic (ADR-013) from an editable per-purpose template; three input kinds — questions (the checklist), reason (an editable menu + free note), text (a free-text box the user writes then refines with AI). The optional polish still sits on top and is checked (ADR-027) — **extended**: `extract_values`/`missing_values` now verify every price/number/date the user typed survives the polish **verbatim** (a reformat `160€`→`160 €` is treated as an alteration, so rounding can never slip), blocking the AI version exactly like a dropped question — the model may never invent, alter, reformat, or round a commitment. Backward-compatible (no `purpose` == `ask`; response keys preserved); never sends; never silently bins a client (rejection is a human-reviewed draft with a chosen reason). Extends ADR-013 & ADR-027; supersedes nothing. |
| [035](adr-035-dark-mode.md) | dark-mode | App-wide **dark mode**, themed entirely at the **token level** (no per-component dark rules): the validated dark palette under `:root[data-theme="dark"]` (graphite surfaces, lighter steel accent, CVD-checked dark counterparty trio `#219980`/`#6E85DE`/`#BA8628`); a pre-paint inline `<head>` script stamps `data-theme` from the saved choice or `prefers-color-scheme` (**no flash, OS-aware, remembered**); a nav **moon/sun toggle** flips + persists it. Every raw light surface/tint in the shared shell + Fila was tokenized (`--surface2`, `--int-bg/-line`, `--purple-bg` added), and a test forbids `background:#fff` from re-appearing, so components recolour for free and dark can't regress into white patches. **Shipped:** the shared shell (nav + whole component kit) + Fila, both verified. **Deferred (honest):** Projetos' spec editor and the legacy /inbox report still carry raw light hexes — a follow-up sweep that doubles as a light-palette-consistency pass. Extends ADR-033/-034. |
| [036](adr-036-speech-act-obligation-fila.md) | speech-act-obligation-fila | The Fila groups by a folded **obligation** (from a new orthogonal `speech_act` axis: ASK/OBLIGATION/FYI/ACK/CLOSE/UNKNOWN), not a response clock: «A cobrar» is genuine billing only, inbound bills get «A pagar», declines/acks self-close, notifications go quiet. Clock only colours+sorts. Legacy fold until a user-run `triage --full`. Refines ADR-029/-033; supersedes ADR-034's «N a cobrar» copy. |
| [034](adr-034-fila-chrome-fronts-and-scoped-rail.md) | fila-chrome-fronts-and-scoped-rail | Reframes the Mesa's chrome (nav · command strip · left rail) around one rule — **critical demand dominates, orientation guides, ambient whispers** — and one invariant: **no number appears without its scope visible in the same gestalt group**. Counterparty **fronts become hero status-cards** whose own per-front demand («N a responder · N a cobrar») lives inside the button (calm «em dia» at zero); the abstract strip headline + tab pills are deleted. Rail counts are **scoped to the active front** («Vistas · Fornecedores») killing the 58-vs-32 contradiction; each vista gets a **stroke glyph** and the keyboard digit moves to a **hover-only** chip; facets **hide when they don't discriminate** («Sem dono 121/121»), and «rever N» moves out of the strip into the rail. P5b (in progress): icon-per-lens nav, **demand**-not-total Fila badge, gear for Admin+densidade, freshness-as-sync pill. Framing-only — no change to rows, dossier, data model, or invariants. Extends ADR-033. |
| [033](adr-033-fila-mesa-com-foco.md) | fila-mesa-com-foco | The Fila becomes **«Mesa com Foco»** (owner-approved 2026-07-23, lands in phases): a full-width split-pane cockpit — bounded group-collapsed queue left, auto-mounted evidence **dossier** right reusing the existing thread renderer (one render path) — with the deterministic **risk tuple as the default order**, **counterparty fronts as first-class tabs** (Hoje · Clientes · Fornecedores · Leads — a hard owner requirement that *refines* ADR-029: obligation stays the partition *within* each front), five **fixed vistas** (no view builder), act-and-advance verbs with printed keys, dashed LLM chips (€/prazo) that may tiebreak but never outrank the clock, repeated-label suppression (trust = dot until focus), the ADR-023 poll + freshness stamp ported to the hero, a **vertical conversation timeline** whose labeled silences terminate in an «agora» band colored by the clock (the response debt made visible), **Adiar** that wakes on date OR new inbound (workspace v9), and bulk verbs that structurally exclude IGNORE. Rejected: one-thread-only mode, briefing grids, money-primary sort, screeners, chat, any second queue surface. |
| [032](adr-032-output-language-and-email-translation.md) | output-language-and-email-translation | Two language features. **(A) Compose in PT/EN/FR/ES**: the deterministic base stays Portuguese (ADR-013 intact); a non-PT choice turns **Melhorar com IA** into a translate+polish pass (`IDIOMA DE SAÍDA` directive). The money/number/date guard is **language-independent** and still runs verbatim across the translation; the verbatim *question* check can't cross languages, so a non-PT result is marked `translated` / **"traduzido — rever"** rather than claiming coverage it didn't verify. **(B) Translate received emails to English**: a display-only reading aid — one `traduzir (EN)` button in the shared `msgHTML` renderer + a capture-phase delegated handler covers Fila/Projetos-Origem/Para-ti; new `translate.py` + editable `translation_playbook.md`; `POST /api/translate` with an in-memory `(message_id, sha256(text))` cache (0 tokens on re-click, cold on restart, nothing persisted). Egress is the same posture as the triage/spec passes (ADR-012/-020); bodies/translations are never logged. Extends ADR-013/-027/-031; supersedes nothing. |
| [037](adr-037-related-list-reserves-entity-slots-drops-deadline-key.md) | related-list-reserves-entity-slots-drops-deadline-key | The Fila related-list stops letting a prolific contact's routine traffic (`by_contact`, any topic) crowd out a rarer, more specific `by_entity` match — measured on the real corpus, 15% of threads with a genuine entity match had it fully hidden by the 8-item cap. Up to 3 entity-match slots are now reserved before `by_contact` backfills. `deadline` is dropped as a standalone match key (40% of its cross-thread matches were two unrelated clients sharing a calendar date, not a relation). «Mesmo contacto» now only ever means the same EXTERNAL party: `by_contact` was previously seeded from the dominant message's raw sender, which is an internal @lindoservico.pt mailbox whenever that message was outbound (31% of threads) — the worst internal address flooded 257 unrelated threads in as "related". Each related item now carries its match reason + the other thread's own momentum, rendered as a badge + status dot. Refines ADR-034. |
| [038](adr-038-mail-account-attribution.md) | mail-account-attribution | Which of **our** inboxes a message reached is now persisted (`sync.message_scope`, keyed by canonical `message_id`) — the durable fact a per-user visibility layer filters on. The scope token is the **address**, not a configured account id (6 real inboxes we never fetch are thus grantable). Three evidence tiers recorded per row: `fetch` (FACT, the account we authenticated as) · `header` (FACT, the server's Envelope-to/Delivered-To, 449 msgs) · `participant` (INFERENCE, one of our addresses in From/To/Cc when no delivery header survived, 102 msgs); absent = UNKNOWN → `sem-atribuicao`, **admin-visible, never everyone-visible**. Attribution may only be **upgraded** (`participant`<`header`<`fetch`), so the idempotent backfill is safe beside a live fetch. A thread's scope is the **UNION** of its messages' — 172/373 real threads reach >1 inbox, so any narrowing rule would hide ~46% of the queue from a legitimate reader; an empty scope fails **closed** for non-admins. Best-effort at fetch time (`scope_record_failed` audited, never breaks read-only IMAP). Lives in regenerable `sync.db`, not precious `workspace.db`. Contains **no policy** — `scopes.visible()` is the seam. Verified on real data: 947 rows / 10 inboxes / 0 unattributed. Extends ADR-016; prerequisite for ADR-021. |
| [039](adr-039-people-auth-and-the-default-deny-gate.md) | people-auth-and-the-default-deny-gate | The app's **first auth layer**. `people` (workspace v10) is the ONE assignable-identity namespace — login is an *attribute*, and someone who cannot sign in **must** name a responsible user (a DB `CHECK`, so assigned work always lands in a signed-in human's view). Uniqueness runs on an NFKC+casefolded `name_key`, not `COLLATE NOCASE` (ASCII-only would have made "Luís"/"LUÍS" two people). Credentials live in a separate `auth.db`; **a session is a row, not a signed blob** — opaque 256-bit token, SHA-256 at rest, so logout / log-out-everywhere / password-change-kills-sessions all work, and there is **no server secret and no new dependency** (stdlib scrypt). The gate is **default-deny middleware** with a closed allowlist, pinned by a route-tree test that fails when any non-public path is reachable signed-out; `/api/*`→401, HTML→303 with an open-redirect-guarded `next`; identity re-read every request. `/healthz` stays public and `bin/healthcheck.py` tries HTTPS-then-HTTP — a gated or HTTP-only probe would mark the container unhealthy and stop `intake-bot` with it. TLS + LAN bind are **opt-in** (`bin/make-cert.sh`), so tests and dev stay on loopback HTTP. Contains no *authorisation* — `person_scopes` + `/api/me` are the seam for Phases C/D. Graduates ADR-021; amends the CLAUDE.md loopback clause ("never public" unchanged). |
| [040](adr-040-the-first-authorization-check-and-the-honest-refusal.md) | the-first-authorization-check-and-the-honest-refusal | **What happens when the app refuses you** — server-side it must refuse correctly, client-side it must say so. (a) *Authorization*: `is_admin` was a column nothing read, so **every** login could `POST /api/admin/accounts` — the route that rewrites `imap.accounts` in `settings.json`, i.e. anyone with an account could repoint which mailboxes we authenticate to. The admin surface is now a **closed path set in the same default-deny middleware** (`_ADMIN_EXACT`/`_ADMIN_PREFIX`), not an `@admin_required` decorator, for ADR-039 §5's reason one layer up: a new `/api/admin/*` route is protected by *existing*. Pinned against the real route tree **in both directions** (adding an unlisted admin route fails; so does removing the gate). A signed-in non-admin gets **403, never a 303 to `/login`** — bouncing someone to a page they would pass instantly misnames the problem; HTML gets `forbidden_page()` in the normal shell (nav intact, reason stated, **no admin data**). Every decision lens stays open to non-admins; demotion bites on the next request (nothing role-shaped is cached). (b) *Honest refusal*: `rows = (d.rows||[])` turned a **401 into «✓ Tudo tratado · 0 a responder»** — the zero-hallucination doctrine escaping the classifier and surfacing in the UI, and the one false claim a person acts on by walking away from waiting work. One shared `fetchJSON()` now raises on non-2xx/non-JSON/network failure, so 401/403 is an **event, not data**; the page raises a **«Sessão terminada» curtain** over the stale view («o que está por trás desta janela pode já não ser verdade») instead of re-rendering, and clears `_timers` so a tab left open stops firing ADR-023's 30 s poll. Proven in a real Chromium, because `TestClient` never runs the JS. Extends ADR-039 (amended there), supersedes nothing. Known limits: `is_admin` is one boolean, not a role model; per-person **visibility** is still unenforced (Phase D). |
| [041](adr-041-the-roster-becomes-people-and-a-person-owns-their-account.md) | the-roster-becomes-people-and-a-person-owns-their-account | Completes Phase C. **The link now agrees with the gate**: ADR-040 locked `/admin` and left the gear still offering «Administração» to everyone, so a member's only feedback was a 403 they had to walk into, once per lens, forever — `page(person=…)` renders it for admins only, and `person=None` is default-deny so a builder that forgets costs an *admin* their link (visible) rather than showing a member a locked door (silent). The shell **says whose session it is** (escaped name · role in words · POST sign-out, because `/logout` revokes the session ROW). **«A minha conta»** gives a person their own surface — change your password (current one required; the fresh session is re-minted because `set_password` revokes every session including yours), end the other sessions, see them listed. `must_change` was a column nothing read since ADR-039, so a "temporary" password was a promise the app never kept: `auth reset` now sets one and the **middleware funnels** until it is replaced (303 for HTML, 403 with a reason for `/api/*`; `/logout` stays public, so it refuses without trapping). **One roster**: `_roster()` is `people`; `settings.team` ∪ the legacy `roster` table become a seed folded in idempotently as assignable-only, re-run right after `/setup` (on a real first boot there is no admin to be accountable yet) and never touching an existing or *deactivated* person. A **«Pessoas»** panel replaces hand-written SQL against the precious store — every rule in `workspace.py`, not the route, chief among them **the install can never reach zero active admins** (`/setup` 404s once a credential exists, so that state is unrepairable from the app); **deactivate preserves who decided what, remove is only for names with no history** (name-as-key cannot cascade), and removal purges `auth.db` too (two files, no cross-file FK). A scope grant is **validated against the inboxes that exist** — an unvalidated typo reads as a permission and matches no mail. Invite links are minted **in the browser**, so the single-use token stops living in shell history. `fetchJSON` now carries a refusal's REASON on the thrown error (still never returning a body), because ADR-040's seam flattened «Rita já existe» to «falhou». Two defects found by *looking at the render*: buttons on your own row that the server always refuses, and a `#fff` label washed out on the dark theme's pale accent. Known limits unchanged: one boolean role, and visibility still unenforced (Phase D) — which the account page states rather than implies. |
| [043](adr-043-header-decoding-owns-the-raw-8bit-case.md) | header-decoding-owns-the-raw-8bit-case | A real client subject rendered as `Pedido de or??amento ??? constru????o` — one U+FFFD per **byte**, which is the whole diagnosis: the sender wrote the header as **raw 8-bit UTF-8** (no RFC 2047; out of spec, but 2 of 553 corpus messages do it, both client mail), and `email.message_from_bytes` had already destroyed it — compat32 sanitizes such a header into `Header(charset="unknown-8bit")` **inside `msg.get()`**, before any decoding code can run. `envelope._decode_header` was correct and simply never saw the bytes. New `headers.py` owns "header value → text" for **both** id derivations (so `envelope.parse_eml` and `identity.canonical_id_from_raw` cannot diverge — the property `identity.py` exists for): parse via `RAW_HEADERS` (a `Compat32` whose `header_fetch_parse` returns the value unchanged, everything else identical) so the bytes survive as surrogate escapes, then decode — declared charset first for encoded-words, UTF-8 → cp1252 → latin-1 for raw 8-bit. **No mislabel heuristic**, because it was measured and there is nothing to fix (0 failing, 0 mislabelled across 1029 charset-labelled headers) — guessing against an explicit label is how correct mail starts rendering wrong. Mixed encoded-word + 8-bit headers decode per chunk via `raw-unicode-escape` (the exact inverse of what `decode_header` does to plain runs). **Invariant: no surrogate leaves the module** — sqlite3/`json.dump` raise on one, which would turn a rendering bug into a write failure. Fixed two latent aborts from the same cause: `canonical_id` doing `.strip()` on a `Header` (**AttributeError killing a whole fetch**) and an uncaught `HeaderParseError` on a malformed encoded-word. Verified by diffing all 553 envelopes old-vs-new: 2 messages changed in 3 fields (`subject`, `to`, and the attachment's mojibake download name), `message_id` in **zero**. |
| [042](adr-042-the-app-sends-exactly-one-kind-of-mail.md) | the-app-sends-exactly-one-kind-of-mail | **The app gains an outbound mail path — one message, and it is a password-reset link.** The owner locked himself out twice in an hour with `auth reset` at a shell as the only way back, because he is the sole admin. The assumed blocker turned out not to exist: ADR-002 binds *the IMAP client and the mailboxes we triage* (`EXAMINE`/`BODY.PEEK`, no `STORE/EXPUNGE/DELETE/APPEND/COPY`) and says nothing about sending, so a dedicated write-only account that never opens IMAP **sits beside that guarantee rather than overturning it** — pinned by a test that greps `mailer.py` for the forbidden verbs, because the failure to prevent is a future APPEND-to-Sent. The real cost was never SMTP: `people` had **no address anywhere**, so this is a **precious-DB migration (v10→v11)** plus an admin surface, then a transport. `people.email` is **never inferred** from `person_scopes` (inboxes someone READS) or `imap.accounts[]` (mailboxes we FETCH) — a guess mails a credential-bearing link to the wrong human — and one address may belong to only one person. The token is an invite with a shorter fuse (`password_resets`, opaque 256-bit, **SHA-256 at rest**, atomic single-use `UPDATE … WHERE used_ts IS NULL`, **30 min** vs 72 h, minting one kills the previous); redeeming **revokes every other session**, because "I forgot" and "someone else is in my account" are indistinguishable and the safe reading is the second. The public `POST /recuperar` is **byte-identical** for a real name, an invented one, one with no address, a throttled one, and one whose mail failed — the roster is people's real names and must not be enumerable. The link is built from **`mail.base_url`, never the request `Host:`** (reset-link poisoning). The throttle bounds **mail, not logins**, so it can never lock anyone out. No mail configured → **503 «indisponível», naming the admin route that works**, not a fake «check your email» (ADR-040's honest refusal). Verified against the real server: 465 + 587, TLS 1.3, `CN=lindoservico.pt`, SPF `ip4:185.12.116.228` matching the MX and **DMARC p=reject** (so `From:` alignment is refused at construction, not discovered as silent non-delivery) — probes sent **no message**. Telegram was NOT made an auth factor (ADR-021 R6 stands). Links point at the LAN over **self-signed TLS**, cert-warning cost stated; **"never public" unchanged**. Known stale-config risk: the host is a DHCP client, so `auth mail-test` warns when `base_url` drifts. **Explicitly not a zero-admin repair** (ADR-041 §10). Extends ADR-039/-041; scopes ADR-002. |
| [044](adr-044-inicio-the-landing-page-is-a-decision-not-a-cockpit.md) | inicio-the-landing-page-is-a-decision-not-a-cockpit | **«/» is Início, a landing page; the Fila moves to `/fila` and is otherwise untouched** (no redirect — that would be the same regression wearing a 303). **Amended 2026-07-27:** the original claim that *every* `?tab=`/`?thread=` deep link survives was **false**. `syncURL()` only governs the links the Fila writes for **itself**; six **cross-page** links hard-coded the root prefix and landed on Início — which reads no query parameter — silently dropping the clicked conversation (`contrapartes_page` ×3, `para_ti`/`para_ti_page` ×2, `fila_page`'s related-jump fallback). All now name `/fila` explicitly. It shipped silently because the e2e test asserted the **proxy** `location.pathname=='/'` (still true after the move, while the user still lost the thread) and three of the six had no test at all. Fixed alongside: the auth gate sent only `request.url.path` into `?next=`, so a signed-out click on a deep link came back to a bare queue — the login page's own docstring already promised that round-trip. Stale bookmarks are accepted as lost by design (no outbound artifact has ever carried a cockpit deep link). Owner feedback: *«the main page has too much information on first impact»* — measured, arriving at the Mesa presented ~300 discrete items at once (4 hero cards, a 12-entry vistas rail, 56 rows, and a **fully mounted dossier** for whatever sorted first). Right screen to *work* in, wrong screen to *arrive* at. Início is **four big buttons and no rows**: the three counterparty fronts (→ `/fila?tab=…`) plus Para ti — chosen over a by-destination menu (a nav bar in bigger fonts) and a by-decision layout, because the counterparty is how the shop divides its day and ADR-034 already validated that language. **Exactly two highlights earn a place**: the demand split («N a responder · N a aguardar») and the longest-stalled thread **we owe** — `€ em jogo`/`Prazos` stay in the Fila's rail *deliberately*, because every individually-defensible addition is how the Mesa got to 300. «A mais antiga» filters on `owes_reply` first: the oldest row overall is usually an ancient FYI nobody awaits, and reporting it would put a large, permanently **un-actionable** number on the landing page. **Demand is now defined once** — it had been written three times (`_nav_counts`, `respondCount`, `chaseCount`) and Início would have been a fourth, with three of them on screen in the same viewport; `cockpit.respond_demand`/`chase_demand`/`oldest_owed_hours`/`home_summary` own it and `_nav_counts` calls them, with a test that **reads the Fila's JS source** and fails if either side changes alone (a «keep in sync» comment is not a mechanism). Every block is counterparty-scoped and the headline is labelled HOJE (ADR-034's rule, so no count can be misread as a global total); **calm at zero**; **no fetch on load** (embedded server-side, `/api/inicio` only repaints after a sync — a landing page whose first impression is a spinner has failed). Início is not a lens, so no `nlink` claims it — the **logo** becomes the door and carries the active state, because a header with nothing marked reads as «you are nowhere». The restraint is **asserted, not assumed**: `test_the_page_is_the_four_buttons_and_carries_no_queue` fails if the Mesa, the rail, the dossier or a row list reappear. Known limits: the Fila's JS keeps its own copy of the predicates (pinned, not unified); per-person visibility still unenforced (ADR-040 Phase D). |
| [045](adr-045-per-person-visibility.md) | per-person-visibility | **Phase D: `scopes.visible()` finally gets a caller.** ADR-038 recorded which of our inboxes each message reached and said plainly it held no policy; until now that seam had **zero** callers, so every signed-in person saw every thread, body, attachment and project while «A minha conta» listed inbox scopes as editable. **One blocker had to clear first, and it was a live "never-silently-bin-a-client" violation waiting to happen:** `_known_scopes()` validated grants against `imap.accounts[]` (**4** addresses) while `message_scope` held **10** — mail reaches an inbox by Cc, forward or alias — and **22 of 374 real threads (5.9%) carried none of the 4**, with exactly **one** thread in the whole corpus carrying `sem-atribuicao`, so the admin bucket did not catch them either. The vocabulary is now what mail ACTUALLY reached ∪ what is configured; typos still refused. The filter sits at ONE choke point and filters `ints` **before** `build_fila`, never `rows` after — everything downstream (summaries, entity join, «relacionados») is recomputed from `ints`, and a filter that hides the row but keeps the pointer is a leak with extra steps (measured: **26** related entries pointing at unopenable threads before the second gate, **0** after). `person` is a **required keyword with no default** — not even a safe `None` — so a forgotten call site is a TypeError, not a silent inheritance of someone else's visibility. Routes the row-filter cannot reach are closed individually: `/api/thread` (bodies), `/api/relations` (entry **and** each returned bucket), `/api/attachment` (raw bytes, no crm join) — all **404, never 403**, because a 403 confirms the thread exists. **Projects** (23 id-bearing routes) are gated in the **middleware** by one path rule, so the 24th route inherits it; visibility is **derived from their threads** (owner decision), and a project with no threads is admin-only — `project_owners` was rejected because its `owner` is free-text NAME, not `person_id`. **`/inbox` is now admin-only** (owner decision): it renders every body, joins no crm, takes no `person` — honest and total beats half-filtered. **Counts are filtered too** (nav, needs-review, Contrapartes stats+timeline, open-projects), including the trap that `_nav_counts` runs INSIDE the gate to draw the 403 page where `request.state.person` is not yet assigned. An **empty queue says why**: «Sem caixas atribuídas», never «✓ Tudo tratado» — ADR-040's dishonest refusal one layer down — and deliberately does not name «Administração → Pessoas», a door a member cannot open (ADR-041). Scope map cached by store **mtime**; a stale map can only be narrower, never wider. Verified on the real corpus (admin 374 / scoped member 67 / no grants 0, strict subsets; forbidden thread → 404 on every route) **and in Chrome**, which caught a defect no Python test could: the empty-state branch first referenced a `D.no_scopes` object this codebase does not have — embeds are `const NO_SCOPES` — and would have shipped as a ReferenceError. Known limits: `is_admin` is still one boolean; **captures have no scope concept and stay unfiltered**; and all four staff share the IMAP passwords in one `.env`, so this scopes the APP, not the mail server. Consumes ADR-038; extends ADR-039/-040/-041. |
| [046](adr-046-the-attachment-funnel-bands-files-by-what-they-are-for.md) | the-attachment-funnel-bands-files-by-what-they-are-for | **A thread's files become one deduped, banded list instead of 133 chips in MIME order.** Three bands — `FICHEIROS` (a real attached document), `IMAGENS` (pasted into the body — usually *is* the request), `ASSINATURAS` (logos/social icons, **collapsed behind a visible count, never dropped**, which keeps this inside the never-silently-bin rule rather than beside it). Every band is an **INFERENCE** — nothing in MIME says «logo» — so each item renders `band_evidence`, the rule that fired and the measurement that triggered it. **Dedup is sha256 of the decoded bytes, never the filename**: measured 784 byte-identical dupes vs 603 same-name, and **220 pairs sharing a name with different bytes** (`composition.pdf` at 154 KB *and* 152 KB in one thread) — a name key both misses dupes and merges two different documents; the same key had been deciding whether to keep an empty-bodied Trash copy, where a collision deleted a whole message card. **The rule was wrong until it was looked at**: banding by size alone buried **11 distinct real-content images** — a 431×361 CAD drawing with dimension annotations, two order summaries (€59,34 / €6.578,38), a RAL 5013 swatch, a payslip chart, a bank screenshot. No size threshold separates a 377×377 logo from a 353×336 colour swatch; **aspect ratio does** (widest content 1.79:1, narrowest banner 2.14:1), hence the postcard arm. Cost: 3 squarish logos show inline — deliberate, because **a leaked logo costs a glance and a buried drawing costs a quote**. **Three ways to demote high-res social icons were measured and all three rejected**: density (a 2437×2441 CAD duck scores 0.0117 B/px, *below* the Instagram icon), filename (Outlook renames everything), and anchor-wrapping (a stone supplier's whole catalogue is link-wrapped) — the leak is bounded by sort order instead, and the duck is pinned by a test. **The thing not to get wrong**: `src.index` indexes the `_attachments` walk that `attachment_part` re-walks, so banding is additive metadata only — a filter that reindexes repoints every 📎 at a different file *under the right name*. Verified by **bytes, not inspection**: every item of every attachment-bearing thread refetched and hashed — **1039/1039 match**. That sweep also found two pre-existing defects in the funnel's dependency path: `/api/attachment/{message_id}/{index}` **404'd for 201 of 1039 links** because Outlook Message-IDs contain `/` and the ASGI server decodes `%2F` before routing (now `{ref:path}`, split on the last slash), and a non-ASCII filename emitted a latin-1 `Content-Disposition` that is not valid UTF-8 (now RFC 6266 `filename*`) — in a pt-PT shop the accented name is the common case. UI is in the **shared kit**, so Para Ti and Projetos inherit it; previews are lazy and size-gated at 256 KB (worst thread 6.31 MB → 0.45 MB). |
| [047](adr-047-the-signature-belongs-to-the-person-not-the-playbook.md) | the-signature-belongs-to-the-person-not-the-playbook | **A reply draft leaves for the person's own mail client, closed in their name.** «✉ Abrir no mail» hands `to` + `Re: <subject>` + body to the OS default client via `mailto:` — which *opens a composer* and cannot send, so [ADR-002](adr-002-read-only-imap-guarantee.md)/[ADR-042](adr-042-the-app-sends-exactly-one-kind-of-mail.md) are untouched — replacing copy→switch→find→paste→retype-the-address. `Re:` is added only if absent (`re|rv|res|fw|fwd|enc`), and the body is `encodeURIComponent`d because a subject containing `&` silently truncates it. **The closing moves out of `config/reply_playbook.md`, where it was a fixed «Com os melhores cumprimentos / Lindo Serviço» that made every person's mail identical, and becomes `people.signature` (v12) — a TEMPLATE, not rendered text**, so a rename updates every future draft instead of leaving stale details in a block pasted once. Vocabulary is **closed** — `{nome}` `{cargo}` `{telefone}` `{email}`, every value from the person's OWN row, an unfillable token refused **at the store** so the CLI inherits it: a signature is identity, and an invented value is a lie in someone else's name. **A line whose placeholders are all empty is dropped** (`Tel.:` with no number reads as sloppiness), which is also why «A minha conta» renders a preview — you cannot tell what the block looks like by reading the template. **Strip-then-append**, with a regex deliberately NARROWER than `envelope._CLOSING`: that one matches «Obrigado.», the last line of the ask/follow-up templates, and reusing it would have deleted the thanks from every client email. **An empty rendered signature strips nothing** — removing a closing and leaving none is worse — which is what makes `person=None` degrade to the exact pre-ADR-047 company closing. **Signed AFTER the memo, never inside it**: `_reply_cache` keys on the SPEC, so a cached signed draft would hand the next reader the previous reader's name and phone. The stream **holds back its tail** for the same reason it cannot retract what it sent. `people.email` stays admin-set (ADR-042): changing your job title is a Tuesday, redirecting your own password recovery is not. **Projetos is deliberately excluded** — its drafts pass through the ADR-027 polish, which may reword prose, and a model-paraphrased contact block is the confident wrongness this project refuses. |
| [048](adr-048-recurring-branding-art-is-omitted-from-the-attachment-funnel.md) | recurring-branding-art-is-omitted-from-the-attachment-funnel | **Signature art we send is omitted from the funnel entirely — on cross-thread recurrence, not on the sender's domain.** ADR-046's bands read one part in isolation, so a 4322x4320 Instagram icon sat in the visible band with `image001.png`…`image004.png` in front of the file being looked for. The blunt rule ("images from `@lindoservico.pt` are signature art") is **measurably wrong**: the same mailboxes send an annotated 431x361 CAD drawing, a press-kit slide and product photos under identical `imageNNN` names. The signal that separates them is how many **distinct threads** the same bytes ride into — branding travels with every signature (41 threads for the social icons, 5 for the animated footer), content stays in one conversation (content tops out at **2**, so `BRANDING_MIN_THREADS = 3` sits in a measured gap; **message** count does *not* work — the drawing and the footer are 5 messages each). The register (`crm.db` v6 `asset_spread`, rebuilt whole by `build_crm`) covers `cid:`-referenced `image/*` from `OUR_DOMAIN` only, is band-blind, and matches on content hash so an omission survives a forward back. **This narrows ADR-046 §1 "nothing is ever dropped"** — chosen with the trade stated; `email2data assets status` is the audit that replaces the click-through, and it exits 1 rather than print a clean-looking empty list. Fail-open: no register ⇒ nothing hidden. The per-message 📎 chips keep **every** part — the drop happens in `fold_thread`, never in `message_parts`, or every remaining link serves the wrong bytes under the right name. |
| [049](adr-049-the-folder-list-is-discovered-not-configured.md) | the-folder-list-is-discovered-not-configured | **The folders to fetch are asked of the server on every run, not read off a hand-written list.** `settings.json` pinned 78 folders for `orcamentos`; the server had 82 — and a client reply («Proposta 2600476», `Cc: orcamentos@`, 2026-07-29 20:02) was filed into `INBOX.orcamentado` between two polls and was **absent from every store**: no error, no audit line, no gap in the UID cursor, every downstream store internally consistent. That silence is the defect — the only way to notice was a human remembering an email. Measured across all four accounts: **52 of 133 server folders were never opened**, holding 190 messages since 20 Jul, **63 of which the app had never seen** (`luis:INBOX.Sent` 41 · `Pedro:INBOX.spam` 9 · `orcamentos:INBOX.orcamentado` 3 — the 63 is not the 190 because Outlook mints a **different Message-ID for the Sent copy**, which inflates a naive join by 15). Now: IMAP `LIST` every run, `\Noselect` containers skipped, **junk and only junk excluded** (last path segment or the RFC 6154 `\Junk` attribute, editable via `imap.exclude_mailboxes`). **Trash is deliberately NOT excluded** — deleted mail is still evidence a client wrote to us. The pinned list survives as **escape hatch** (an explicitly named folder beats the junk filter) and **fallback** — a failed `LIST` returns `None`, distinct from `[]`, so a server quirk can never narrow the fetch below what a human configured. INBOX sorts first so the `max_messages` cap cannot bite it. The selected set is **audited**, because "which folders did we even look in?" was the first question and the log could not answer it. Upholds [ADR-002](adr-002-read-only-imap-guarantee.md): `LIST` opens no mailbox, and the new tests assert `readonly=True` on the wire rather than only grepping the source. Costs accepted: Sent is now fetched for every account (making outbound direction FACT, not an inference from the From domain), a bounded one-time backfill, and `INBOX.Drafts` now discovered everywhere — left in because the instruction was "spam only", and it is a one-line config edit to drop. The Admin panel changed with it: «Caixas de correio (78)» over 78 rows read as the whole scope while 82 were opened. |
| [051](adr-051-a-reply-we-can-see-discharges-an-owed-reply.md) | a-reply-we-can-see-discharges-an-owed-reply | **An obligation to REPLY is discharged by the observable fact that we replied — not by the classifier agreeing our mail was an answer.** Reported off a rendered dossier: the header read «devemos resposta há 2 dias» with our own 14:49 reply visible in the timeline underneath a chip reading «sem resposta há 2 dias». Both numbers were correct for what they measured, which is the defect. `derive_obligation` folds from the last **decisive** act; our reply was an update («we ran tests, new date 08-07») that Gemini reads — correctly — as `FYI`, and ADR-036 §2's "`FYI`/`UNKNOWN` never override a live move" is not directional, so the 07-31 `ASK` stayed live and `_obligation_since(OWE_REPLY)` kept anchoring to `last_inbound_date`. **No number of replies could ever move that clock.** The rule was written for INBOUND mail — to stop a newsletter wiping a client's question — and applied to our own outbound it made every update-shaped answer a no-op. **The sharpest evidence it is a regression:** `_legacy_obligation` still carries `last_outbound >= last_inbound → AWAIT_THEM` and gets this thread RIGHT, so spending Tier-1 tokens on `triage --full` made the queue placement WORSE than not spending them — Stage 2 dropped that guard and never replaced it. Now: any **outbound** after the decisive message → `AWAIT_THEM`. Deliberately narrow on three axes — `internal` does not count (a forward to a colleague is not an answer to the client, keeping ADR-036's internal-forward fold intact), `OWE_PAYMENT` is unreachable by it (**a mail never pays a supplier**; «A pagar» survives "recebido, pagamos dia 10"), and inbound `FYI` still never overrides a live ask. Rejected: teaching the playbook to emit `ACK` for an answering reply — it puts a per-message LLM judgement in front of a fact we can already observe, and fails silently every time the model reads an update as an update; and anchoring to `max(last_inbound, last_outbound)`, which fixes the number and leaves the label lying. **Measured on the live corpus** (675 threads): **6 move**, all `OWE_REPLY`→`AWAIT_THEM`, all CLIENT; «Precisam de resposta» **36 → 30**; the active Fila is **185 rows before and 185 after** — nothing binned. Cost accepted and bounded: an out-of-office now moves the ball, but the thread stays queued under «À espera deles» and turns «A aguardar» at 72 h, which is the follow-up prompt that case wants. Second, smaller defect closed with it: the dossier timeline paints its debt chip in the segment «agora» → **newest message** and its comment claims exactly that span, but it rendered `clock.age_hours` unconditionally — true only when the clock is anchored there. `thread_clock` now ships `gap_hours` + `anchored_at_last` so the chip speaks as debt only over the segment it owns and falls back to a plain elapsed gap otherwise, still server-computed (no client `Date.now()`, the ADR-034 P5c drift). |
| [052](adr-052-the-project-file-list-spans-every-thread-and-the-intake-channel.md) | the-project-file-list-spans-every-thread-and-the-intake-channel | **«Ficheiros»: a project's files become one list across every conversation AND the intake channel.** The surprising finding, verified in a browser before anything was designed: the cross-thread, content-deduped list **already rendered** — inside «Origem», 381 px of funnel in a 420 px box holding 2427 px of content (16 %). The gap was surfacing, not capability, so ADR-046 §4 is **extended, not superseded** (owner decision: both places). The email half is folded **client-side**, and that is the security decision: `_may_open_project` is ANY-thread, so a server-built project attachment list would hand a member filenames, sizes, page counts and sender addresses for mail they were never granted — the bytes route would still 404, but «Proposta_ClienteX_48k.pdf» is most of the leak. Folding in the client forces **every root through the existing per-thread ADR-045 check**. The intake half **is** a server endpoint (`/api/projects/{pid}/captures`), allowed because captures carry no per-thread scope at all and the timeline beside it has served their thumbnails to this audience since ADR-019 — gated by the same middleware path rule, so it is protected by construction. **Capture media are HASHED** (owner reversal of the brief's `src`-derived ids): a slot-derived id would have made the same drawing, mailed and then re-sent through Telegram, read as two files in a list whose whole promise is one row per file — they now fold through the ADR-046 content key, verified in Chrome as one tile marked `×2`. They land in `FICHEIROS`, not a fourth band (a file that is *both* mailed and captured is one file), but are never laundered into looking like email: own `source`/`channel`/`asserted_by`, own `band_evidence`, `src:{capture_id,index}`, no «ver na fila →», and **every** media index enumerated (a capture with a photo *and* a drawing hid the drawing). A media file missing from the sole-copy store is **listed with `missing:true`**, never skipped. **Two traps closed before they could render:** `attMerge` was first-**block**-wins (`project_threads.added_ts` order) while inheriting `fold_thread`'s "first occurrence supplies `from_email`" wording — harmless until a tile names a sender, at which point it is a confident lie, so the chronological sort is a **precondition** of the provenance line; and `lst.map(_attTile)` would have handed the tile helper the **array index** as its options object the moment it took a second parameter — falsy for tile 0, truthy after — breaking **the Fila and Para Ti, not Projetos**, and rendering. **Para Ti had never rendered the funnel at all** (`loadDetail` discarded `d.attachments`; 8 chips, no `.attf`) — the existing sweep asserted no lens *forks* the kit, never that one *renders* it. One unreachable thread no longer costs the others: the `try` moved inside the root loop and the incompleteness is **said** («⚠ N de M conversas não carregou»), as one merged sentence because a 404 deliberately cannot distinguish "not granted" from "no context". The **preview gate inversion** is decided, not left open: three ~6 MB client photos drew a glyph while the 6 KB supplier logo was the only real thumbnail, so the Ficheiros panel opts into `bigPreviews` (a decision about the surface, still lazy, still server-decided elsewhere). No new table, no migration, no index — 13 projects × 1 thread, 84 items; revisit at >5 threads or >50 previewable items. |
| [053](adr-053-the-corpus-filename-is-the-index.md) | the-corpus-filename-is-the-index | **The corpus filename IS the index.** A live perf diagnosis (2026-08-03) traced every "2 to 10 second" click to Python re-parsing the 850 MB `.eml` corpus on a schedule — `_file_for` globbed and parsed all 1094 files on cold call (9.0 s), `_rebuild_state` called `_idx.clear()` after every sync (every 15 min plus each deploy) so a warm click was rearmed as a 9-second click, and `report.prepare` / `specbuild._corpus_index` did the same scan on the sync path (18.65 s per prepare, adding 2-3 s contention to every click landing in the window). The load-bearing property: `fetch.py` writes each `.eml` under `safe_filename(canonical_id_from_raw(raw)) = sha256(canonical_id)[:32] + ".eml"`, which is a **pure function** of the message id, and `envelope.parse_eml(...)["message_id"]` returns the same canonical id — verified over the whole corpus, **1116/1117 files land on the computed name**. So `corpus_dir / safe_filename(mid)` is not "a candidate" — it *is* the file. Every lookup is now O(1) compute + one filesystem stat at three call sites (`webapp._file_for`, `report.prepare`, `specbuild.rebuild_jobspecs`); the whole-corpus glob-and-parse survives as a lazy one-time fallback for the ~1-in-1000 legacy case (an .eml whose derived canonical id no longer matches its filename). `_rebuild_state` no longer clears `_idx` — content-addressed entries cannot go stale. `report.prepare` also gains a module-level `_env_cache` so a warm sync parses zero envelopes; a typical sync brings ~5 new mids and parses 5, not len(corpus). Measured impact: cold click 9.31 s → 0.42 s (byte-identical payload); prepare's scan burn 9.04 s → 0.01 s; the 15-minute rearmed-click cadence ends. Deferred: moving the sync out of the FastAPI process (subprocess) — this ADR removes both parse-heavy contention windows, which alone eliminates the click-during-sync slowdown in normal operation. |
| [054](adr-054-llm-derived-body-fragments-live-in-out-sidecars.md) | llm-derived-body-fragments-live-in-out-sidecars | **A model-derived body fragment is a derived result, not a log line — it may live in a sidecar under `out/`, and nowhere else.** The gate fila-evidence Phases 4 and 5 sat behind, because they are the first thing in this app that takes a fragment of a client's email, sends it to a model, and writes the result back to disk. Decided by three verified facts: `corpus/` already holds every message as a complete `.eml`, so a one-sentence quote is strictly LESS exposure than the file beside it; the rule that actually bites is about the **log**, since `out/audit.jsonl` is served by no route, sits outside ADR-045 entirely, is not backed up and is never pruned; and `audit.py`'s no-raw-body rule is a **docstring with zero enforcement and zero test coverage**, whose two call-site conventions disagree. So: two sidecars under `out/` (`evidence.jsonl` by message_id, `narratives.jsonl` by thread_root — `out/*` is gitignored at any depth, verified with `git check-ignore`, and a store placed anywhere else is NOT), never `results.jsonl` (body-free by contract), never `crm.db` (os.replaced whole every sync), never the audit log (a **reason code** — `echo`/`not_in_body`/`not_unique`/`value_not_in_quote` — answers "why was this rejected" carrying no body text at all), and **no new route**: evidence rides inside `/api/thread`'s existing `facts` entries and the narrative as one more key on the same single response, so both inherit `_may_open_thread` with no surface to forget. **Not** added to `bin/backup-workspace.sh` — measured, not assumed: `snapshot_and_verify` is sqlite3-only and `DatabaseError: file is not a database` with no `except` makes the WHOLE pre-migration backup exit 1 and print "BACKUP FAILED"; a full rebuild costs ≈$0.68, so losing these is a bill, not data loss. The echo rule (reject `quote == value`) discards nothing, and that is what makes Phase 4 cheap: an echo can only occur when the value is IN the text, and there the Phase-3 client already paints it by searching for the value — the same search. Re-partitioned by whether Phase 3 already paints the row, the paid-for spike reads **89% echo / 0% useful** on painted rows and **27% echo / 70% useful** on dark ones; the handover's "50% of every run is discarded" was an artifact of pooling the two halves. Storing a row for every ATTEMPTED message (including rejections) is likewise not optional — the gate keys on presence, so "store nothing on failure" would re-bill every failed message on every sync, forever. |
